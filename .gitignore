# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Testing / tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Load test + plot output
*.log
results/
plots/

# ...except the recorded load test results. These are the milestone 4
# deliverable, and the raw JSONL is what lets every number in
# loadtest/README.md be recomputed (python -m loadtest --plot-only) without a
# Redis server or a re-run.
!loadtest/results/
!loadtest/results/**

# OS / editor
.DS_Store
.idea/
.vscode/
